if (markBMI > johnkBMI) {
  console.log(
    `Mark's BMI (${markBMI}) is heigher than john's BMI (${johnkBMI})!`
  );
} else {
  `Mark's BMI (${johnkBMI}) is heigher than john's BMI (${markBMI})!`;
}
